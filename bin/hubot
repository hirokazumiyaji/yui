#!/bin/sh

set -e

npm install
export PATH="node_modules/.bin:node_modules/hubot/node_modules/.bin:$PATH"

babel src --presets es2015 --out-dir scripts

exec node_modules/.bin/hubot --name "yui" "$@"
